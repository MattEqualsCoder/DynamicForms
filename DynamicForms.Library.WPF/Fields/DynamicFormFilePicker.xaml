<UserControl x:Class="DynamicForms.Library.WPF.Fields.DynamicFormFilePicker"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:DynamicForms.Library.WPF.Fields"
             mc:Ignorable="d"
             d:DesignHeight="25" d:DesignWidth="300">
    <UserControl.Style>
        <Style TargetType="local:DynamicFormFilePicker">
            <Style.Resources>
                <Style TargetType="Button" />
            </Style.Resources>
        </Style>
    </UserControl.Style>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <TextBox Grid.Column="0" IsReadOnly="True" Name="MainTextBox"></TextBox>
        <Button Grid.Column="1" Name="BrowseButton" Margin="7 0" Padding="5 0" Click="BrowseButton_OnClick">Browse...</Button>
        <Button Grid.Column="2" Name="ClearButton" Padding="5 0" Margin="0" FontSize="10" Width="25" MaxWidth="25" MinWidth="0" Click="ClearButton_OnClick">
            &#x2715;
        </Button>
    </Grid>
</UserControl>
